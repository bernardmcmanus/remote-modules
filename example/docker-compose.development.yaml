version: '3'
services:

  fragment-a:
    command: yarn start
    environment:
      - NODE_ENV=development
    volumes:
      - ./fragment:/src
      - /src/.remote
      - /src/node_modules

  fragment-b:
    command: yarn start
    environment:
      - NODE_ENV=development
    volumes:
      - ./fragment:/src
      - /src/.remote
      - /src/node_modules

  shell:
    command: yarn start
    environment:
      - NODE_ENV=development
    volumes:
      - ./shell:/src
      - /src/node_modules
