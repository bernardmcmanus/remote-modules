/**
 * Returns the last element of an array
 * @since 0.1.0
 *
 * @param {array} target - The target array
 * @return {?}
 */
export default function last(target) {
	return target[target.length - 1];
}
